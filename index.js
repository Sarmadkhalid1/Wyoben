/**
 * @format
 */

import { AppRegistry, NativeModules, Platform } from 'react-native'
import App from './App'
import AppContent from './src/AppContent'

AppRegistry.registerComponent('Wyoben', () => AppContent)
